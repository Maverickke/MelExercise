/*Script 2) 
	Write a script that will move the selected object / objects 1.5 units up (in Y)
	(and keep moving it everytime it gets executed)*/
	
proc moveObjectSelected(){
    string $sel[] = `ls -sl`;
    move -r -y 1.5;
}
moveObjectSelected();